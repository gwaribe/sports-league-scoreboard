# Base URL of the FastAPI backend. The backend serves every endpoint under /api.
# Local development default (matches the backend's dev server):
VITE_API_BASE_URL=http://localhost:8000/api

# For a same-origin deployment where the backend is proxied at /api, use:
# VITE_API_BASE_URL=/api
